# CI build and installed verification tools. Product runtime dependencies remain
# declared in each package's pyproject.toml and are not updated by this lock.
build==1.3.0
setuptools==84.0.0
olefile==0.47
pywin32==312 ; sys_platform == "win32"
